### 1. WSL

wsl --install -d Ubuntu

Note: if you alreay have the wsl. so please don't use this command just use wsl, and in case you use linux or mac skip this step.

### 2. Install & Run Ansible

sudo apt update
sudo apt install -y ansible

- verify

ansible --version

- For SSH, make sure WSL has access to the private key that matches the public key Terraform injected into Rocky

mkdir -p ~/.ssh

cp /mnt/c/Users/Admin/.ssh/id_ed25519 ~/.ssh/id_ed25519
cp /mnt/c/Users/Admin/.ssh/id_ed25519.pub ~/.ssh/id_ed25519.pub

chmod 700 ~/.ssh
chmod 600 ~/.ssh/id_ed25519
chmod 644 ~/.ssh/id_ed25519.pub

- verify ssh working or not
ssh rocky@192.168.100.100